## 1. History preamble for fresh CLI sessions

- [x] 1.1 Add a shared helper in `fleet-snowfluff-ai`'s providers layer that renders `(history: &[Message], current: &str, char_budget) -> String`: header, then `User:` / `Assistant:` turns oldest-to-newest, then the current message; most recent turns win when the budget is exceeded; empty history returns the current message unchanged. Verify with unit tests for: empty history, ordering, budget truncation keeping the newest turns, and a single oversized turn. Landed as `providers/history_preamble.rs` (`render_with_history`, `prompt_for_session`, `HISTORY_CHAR_BUDGET = 6000`); 9 unit tests incl. multi-byte truncation.
- [x] 1.2 Give `ClaudeCodeCli` and `Codex` a `history: Vec<Message>` constructor argument (alongside `resume_session_id`) and build the prompt through the helper whenever no session is being resumed, including on the stale-resume retry path; a successfully resumed session sends only the latest message as today. Extract prompt construction into a pure function per provider so it can be tested without a CLI. Verify with tests asserting: no resume → history included; resume → latest message only; stale-resume retry → history included; no history → nothing added. `ClaudeCodeCli::new`/`Codex::new` take `history`; each has a pure `build_prompt`, and the stale-resume retry builds its prompt with `resuming = false`. 4 tests per provider.
- [x] 1.3 Thread the real history (`context` in `send_chat_message`, not the few-shot-bearing message list) through `route_provider`/`build_provider` and into `FallbackAttempt` so `run_generation_fallback` gives an escalated CLI target the same history. Verify `cargo build --workspace` and the existing `ai_commands.rs` dispatch tests still pass with the new parameter, and that persona few-shot examples never appear in a rendered preamble. `route_provider` gained a `history` parameter (`build_provider` passes none — its callers never chat); `FallbackAttempt` carries `history` so escalation seeds a CLI target. `cargo test --workspace` green.

## 2. Working directory and process termination

- [ ] 2.1 Add a working-directory resolver in the app crate: `project_root` when set and existing, otherwise `<app config dir>/cli-workspace` created on demand; a set-but-missing `project_root` falls back to the application directory and logs it. Verify with tests using a temp dir for: root set and present, root unset, root set but missing.
- [ ] 2.2 Pass the resolved directory into both CLI providers and apply it via `current_dir` on their chat spawns (factor each spawn's `Command` construction into a function so it can be inspected). Verify with tests asserting `get_current_dir()` equals the supplied directory for both providers, and that it is never left unset.
- [ ] 2.3 Set `kill_on_drop(true)` on the `claude -p` and `codex exec` chat spawns only — not on `trigger_login` or the availability checks. Verify with a test that builds a command through the same configuration helper around a long-running stand-in process, drops the child, and asserts the process is gone; and confirm by reading the code that `trigger_login` is unchanged.

## 3. Persisted CLI session references

- [ ] 3.1 Add a sidecar store module in the app crate next to `chat_log_store`: path derivation from a session log path (`<ts>_<id>.sessions.json`), a versioned list of `{ profile, session_id, cwd }` entries, best-effort write, and missing/corrupt files reading as empty. Verify with temp-dir tests for: round trip, missing file, corrupt file, an older/unknown version, and updating one profile's entry without disturbing another's.
- [ ] 3.2 Wire it into `chat_commands.rs` through one shared lookup helper used by **both** `send_chat_message`'s direct branch and `run_generation_fallback`: on a `cli_sessions` miss for `(conversation, profile)`, load the sidecar entry and use it only if its recorded `cwd` equals the resolved working directory. Extend `store_cli_session_id` with the session log path and resolved working directory parameters (it has neither today) and write through on every store, recording the directory used. Verify with tests on the pure decision (matching cwd → resume id returned; differing cwd → none), by confirming by reading that no call site still reads `cli_sessions` directly, and that `cargo build --workspace` is clean.
- [ ] 3.3 Confirm `new_chat_session` needs no sidecar handling (a new log gets a new sidecar) and that the in-memory `cli_sessions` clear there remains correct; verify by reading the resulting flow and by a test that a sidecar belonging to the previous log is not consulted for the new session path.

## 4. Verification and docs

- [ ] 4.1 Run `cargo fmt`, `cargo clippy --workspace --all-targets`, and `cargo test --workspace`; verify no new warnings and no regressions in the existing suite.
- [ ] 4.2 Manually verify against a real logged-in Claude Code, recording results in this file: (a) two turns reuse one session; (b) restart the app and confirm the next message resumes rather than starts cold; (c) change `project_root` and confirm the stored session is skipped and the reply still reflects earlier turns; (d) press Stop mid-reply and confirm no `claude` process remains (`ps`); (e) escalate from `mix`-mode Ollama and confirm the CLI reply reflects turns Ollama answered. Note explicitly that Codex was not verified against a real subscription.
- [ ] 4.3 Update `AI_FEATURES.md` where it describes CLI session behavior, and record the deferred items (`Execution` record, `ContextManager` wiring, `SessionManager`, runtime adapter, process-group kill) in this change's design notes if implementation surfaced anything new. Verify the doc renders and its claims match the shipped behavior.
