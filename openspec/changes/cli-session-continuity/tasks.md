## 1. History preamble for fresh CLI sessions

- [x] 1.1 Add a shared helper in `fleet-snowfluff-ai`'s providers layer that renders `(history: &[Message], current: &str, char_budget) -> String`: header, then `User:` / `Assistant:` turns oldest-to-newest, then the current message; most recent turns win when the budget is exceeded; empty history returns the current message unchanged. Verify with unit tests for: empty history, ordering, budget truncation keeping the newest turns, and a single oversized turn. Landed as `providers/history_preamble.rs` (`render_with_history`, `prompt_for_session`, `HISTORY_CHAR_BUDGET = 6000`); 9 unit tests incl. multi-byte truncation.
- [x] 1.2 Give `ClaudeCodeCli` and `Codex` a `history: Vec<Message>` constructor argument (alongside `resume_session_id`) and build the prompt through the helper whenever no session is being resumed, including on the stale-resume retry path; a successfully resumed session sends only the latest message as today. Extract prompt construction into a pure function per provider so it can be tested without a CLI. Verify with tests asserting: no resume → history included; resume → latest message only; stale-resume retry → history included; no history → nothing added. `ClaudeCodeCli::new`/`Codex::new` take `history`; each has a pure `build_prompt`, and the stale-resume retry builds its prompt with `resuming = false`. 4 tests per provider.
- [x] 1.3 Thread the real history (`context` in `send_chat_message`, not the few-shot-bearing message list) through `route_provider`/`build_provider` and into `FallbackAttempt` so `run_generation_fallback` gives an escalated CLI target the same history. Verify `cargo build --workspace` and the existing `ai_commands.rs` dispatch tests still pass with the new parameter, and that persona few-shot examples never appear in a rendered preamble. `route_provider` gained a `history` parameter (`build_provider` passes none — its callers never chat); `FallbackAttempt` carries `history` so escalation seeds a CLI target. `cargo test --workspace` green.

## 2. Working directory and process termination

- [x] 2.1 Add a working-directory resolver in the app crate: `project_root` when set and existing, otherwise `<app config dir>/cli-workspace` created on demand; a set-but-missing `project_root` falls back to the application directory and logs it. Verify with tests using a temp dir for: root set and present, root unset, root set but missing. Landed as `crates/fleet-snowfluff/src/cli_workdir.rs` (`resolve`, `for_app`); 4 tests: root set, unset, set-but-missing, and a path that is a file.
- [x] 2.2 Pass the resolved directory into both CLI providers and apply it via `current_dir` on their chat spawns (factor each spawn's `Command` construction into a function so it can be inspected). Verify with tests asserting `get_current_dir()` equals the supplied directory for both providers, and that it is never left unset. Both providers take a `working_dir` and build their command through an inspectable `chat_command`; resolved once in `send_chat_message` and carried in `FallbackAttempt` for escalation. Tests assert `get_current_dir()` for resumed and fresh commands in each provider.
- [x] 2.3 Set `kill_on_drop(true)` on the `claude -p` and `codex exec` chat spawns only — not on `trigger_login` or the availability checks. Verify with a test that builds a command through the same configuration helper around a long-running stand-in process, drops the child, and asserts the process is gone; and confirm by reading the code that `trigger_login` is unchanged. Shared `cli_process::apply_chat_spawn_settings` sets `kill_on_drop(true)` (plus cwd and stdio) for chat spawns only; `trigger_login` and availability checks are untouched. Unix test spawns `sleep`, drops the child, and asserts the process is gone.

## 3. Persisted CLI session references

- [x] 3.1 Add a sidecar store module in the app crate next to `chat_log_store`: path derivation from a session log path (`<ts>_<id>.sessions.json`), a versioned list of `{ profile, session_id, cwd }` entries, best-effort write, and missing/corrupt files reading as empty. Verify with temp-dir tests for: round trip, missing file, corrupt file, an older/unknown version, and updating one profile's entry without disturbing another's. Landed as `crates/fleet-snowfluff/src/cli_session_store.rs` (`sidecar_path`, `load`, `upsert`, `usable`); 8 temp-dir tests, including a real sidecar with only its version bumped.
- [x] 3.2 Wire it into `chat_commands.rs` through one shared lookup helper used by **both** `send_chat_message`'s direct branch and `run_generation_fallback`: on a `cli_sessions` miss for `(conversation, profile)`, load the sidecar entry and use it only if its recorded `cwd` equals the resolved working directory. Extend `store_cli_session_id` with the session log path and resolved working directory parameters (it has neither today) and write through on every store, recording the directory used. Verify with tests on the pure decision (matching cwd → resume id returned; differing cwd → none), by confirming by reading that no call site still reads `cli_sessions` directly, and that `cargo build --workspace` is clean. `ChatRuntimeState::resume_session_id` is the one lookup, used by both `send_chat_message` and `run_generation_fallback`; `record_session` writes through. `cli_sessions` values are now `CliSessionEntry { session, cwd }` so in-memory entries are cwd-checked too. `store_cli_session_id` gained the log path and working directory. 7 tests in a new `chat_commands` test module; nothing else reads `cli_sessions` directly (checked by grep).
- [x] 3.3 Confirm `new_chat_session` needs no sidecar handling (a new log gets a new sidecar) and that the in-memory `cli_sessions` clear there remains correct; verify by reading the resulting flow and by a test that a sidecar belonging to the previous log is not consulted for the new session path. `new_chat_session` needs no sidecar handling: a new log path is a new conversation id and a new absent sidecar; its in-memory `clear()` is kept (it only frees memory now). Covered by `a_new_conversation_never_sees_the_previous_conversations_session`.

## 4. Verification and docs

- [x] 4.1 Run `cargo fmt`, `cargo clippy --workspace --all-targets`, and `cargo test --workspace`; verify no new warnings and no regressions in the existing suite. `cargo fmt --check`, `cargo clippy --workspace --all-targets` (no warnings in any touched file), `cargo test --workspace` (51 app + 233 ai + 47 core tests, 0 failed), prettier on the docs.
- [ ] 4.2 Manually verify against a real logged-in Claude Code, recording results in this file: (a) two turns reuse one session; (b) restart the app and confirm the next message resumes rather than starts cold; (c) change `project_root` and confirm the stored session is skipped and the reply still reflects earlier turns; (d) press Stop mid-reply and confirm no `claude` process remains (`ps`); (e) escalate from `mix`-mode Ollama and confirm the CLI reply reflects turns Ollama answered. Note explicitly that Codex was not verified against a real subscription.

  **Results so far (real Claude Code 2.1.281, logged in via claude.ai; Codex not installed, so not verified):**
  - (a) two turns reuse one session — **verified live** (`claude_code_cli_live_two_turn_session_resume`, now run with an explicit cwd).
  - (b) restart resumes rather than starts cold — **verified at the state level only** (a fresh `ChatRuntimeState` reading a sidecar written by another); **not** exercised through a real app restart.
  - (c) `project_root` change — the premise turned out false: Claude Code resumed the same session id from a *different* directory and remembered the earlier turn, so cwd scoping does not apply to it (design Decision 5 updated; the check is kept as the conservative default). The GUI flow itself was **not** run.
  - (d) Stop ends the process — **verified live** (`claude_code_cli_live_dropping_the_stream_terminates_the_process`: `pgrep` finds the real `claude` mid-generation and nothing 3s after the stream is dropped); the GUI Stop button was **not** clicked.
  - (e) escalation carries earlier turns — the mechanism is **verified live** (`claude_code_cli_live_fresh_session_is_seeded_with_history`: a fresh session recalled "durian" from the seeded history); an actual `mix`-mode Ollama-to-Claude escalation in the app was **not** run.

  Still open: a hand run of the app (`just dev`) for (b), (c), (d) and (e) through the real UI. Left unchecked until that is done.
- [x] 4.3 Update `AI_FEATURES.md` where it describes CLI session behavior, and record the deferred items (`Execution` record, `ContextManager` wiring, `SessionManager`, runtime adapter, process-group kill) in this change's design notes if implementation surfaced anything new. Verify the doc renders and its claims match the shipped behavior. `AI_FEATURES.md` gained a "Conversation memory and sessions" section; `design.md` Decision 5 and Risks updated with what live verification found (see 4.2 and 4.4).
- [x] 4.4 Fix found during 4.2: `--allowedTools`/`--disallowedTools` are variadic in Claude Code 2.1.281, so with no `--model` and no `--resume` after them the prompt was swallowed as a tool name and the CLI exited with "Input must be provided", surfacing as an empty reply (this predates the change but blocked every fresh session, which is exactly what this change seeds). `build_args` now ends options with `--` before the prompt, which also stops a prompt beginning with `-` being read as a flag. Verified by two unit tests (no model/no resume; dash-leading prompt) and by all three live tests passing against the real CLI. Codex's argument list has no variadic option and was not changed.

## 5. Follow-ups from verification

- [x] 5.1 A resumed session missed turns another provider answered (`mix` mode): record how many transcript messages each session holds (`seen_turns`, in the sidecar and in memory) and, on resume, send the turns after that along with the new message. `prompt_for_session(history, seen_turns, latest, resuming)`; a completed turn records `history_len + 2`, a failed one keeps the stored count for the same session and otherwise 0. Verified by unit tests (only unseen turns rendered, past-the-end count, fresh session ignores the count), state-level tests (count survives a restart; failed turn does not advance it; a new session id after a failed turn starts at 0), and the fake-CLI tests. The spec, design and proposal were updated to match.
- [x] 5.2 Replace the growing parameter lists with one `CliContext { resume_session_id, history, seen_turns, working_dir }` (providers take it; `route_provider` takes it) and a `TurnCtx { session_path, working_dir, history_len }` for `run_generation` / `stream_to_completion` / `store_cli_session_id`. `ChatRuntimeState::cli_context` is now the single builder used by the direct branch and the escalation path. `cargo build --workspace` clean; existing dispatch tests updated.
- [x] 5.3 Automated coverage for the stale-resume retry wiring: a fake `claude` / `codex` script (via a test-only `cli_locator` override) rejects any resume like a pruned session, answers a fresh call, and records every invocation. The tests assert one stale attempt followed by one fresh retry that carries the history, on both providers. Mutation-checked: making the Claude retry stop seeding fails the test. An escalation test (`an_escalation_target_with_no_session_is_handed_the_whole_history`) covers the shared context builder both paths use.
- [x] 5.4 Only resolve (and so create) the CLI working directory for a CLI-backed profile: `ProviderProfile::uses_cli`, tested; `send_chat_message` passes an unused empty path otherwise.
- [x] 5.5 Design text: Decision 3's signature corrected (returns a `String`). Codex's prompt now follows `--` (unit-tested for fresh and resumed commands, dash-leading prompt); **unverified against a real `codex`**, recorded in Decision 8.
