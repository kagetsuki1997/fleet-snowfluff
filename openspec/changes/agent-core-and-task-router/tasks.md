## 1. Task Router domain types

- [x] 1.1 Add `Task`, `TaskRequirements`, `RoutingContext`, `SessionStrategy` (`None`/`Create`/`Resume(SessionRef)`), and the minimal `ExecutionRoute` (`runtime`/`provider`/`model`/`session_strategy`, no `capabilities`/`RoutingReason` fields yet, per design.md's Decisions) to `fleet-snowfluff-ai`; verify with unit tests that each type round-trips through construction/equality as expected. Landed in `crates/fleet-snowfluff-ai/src/task_router.rs` — `ExecutionRoute` uses the existing `ProfileKey` (provider+auth_method+model already live there) instead of inventing separate `RuntimeId`/`ProviderId`/`ModelId` types with no other consumer; `SessionStrategy::Resume` holds a bare `String`, not the app crate's `ExternalSessionRef`, to avoid `fleet-snowfluff-ai` depending on the app crate. 7 unit tests, all passing.
- [x] 1.2 Add the `TaskRouter` trait (`async fn route(&self, context: RoutingContext) -> Result<ExecutionRoute, ProviderError>`); verify it compiles behind a trait object the same way `AiProvider` does, with a trivial test implementation. Verified via `task_router_trait_is_usable_behind_a_box_dyn`.
- [x] 1.3 Implement a concrete `TaskRouter` that resolves `ExecutionRoute` from `AiSettings.task_router_mode`: `single` always resolves to `default_profile`; `mix` resolves to the enabled Ollama profile if one exists, otherwise to `default_profile` directly (this is knowable upfront from settings, not a runtime failure — see design.md's Decisions for why this stays a one-time call, never re-invoked for escalation or infra fallback). Verify with tests covering all three resolutions (single, mix-with-Ollama-enabled, mix-without-Ollama-enabled). `DefaultTaskRouter` in the same module; required pulling task 2.1 forward (see its own note below) since this can't compile without `AiSettings.task_router_mode` existing.
- [x] 1.4 Wire `send_chat_message` to build a `RoutingContext` and call `TaskRouter::route()` for its initial provider decision, replacing the current direct `settings.default_profile()` lookup; verify with a test that `single` mode's resulting behavior is unchanged from before this task (same provider chosen, same call shape). The pre-flight "no provider configured" check stays exactly where it was, ahead of routing — only the profile _selection_ moved behind `TaskRouter::route()`. `cargo test --workspace`: 186 passed (up from 175), 0 failed, 4 ignored, confirming no regression to the existing `single`-mode path.

## 2. Task Router mode: single / mix

- [x] 2.1 Add `TaskRouterMode` (`Single`/`Mix`) and `AiSettings.task_router_mode` (default `Single`); verify `AiSettings::default()`/`sanitize()` yield `Single` for both a fresh config and a config missing the field, matching the existing field-by-field-degrades-gracefully pattern. **Completed as part of Group 1** (task 1.3 needed the field to exist to compile/be tested) — `TaskRouterMode` lives in `settings.rs` alongside `AuthMethod`. 4 dedicated tests plus every existing `settings.rs` struct-literal site updated (`migrate_legacy_active_provider`'s two branches, `sanitize`, and the app crate's `ai_commands.rs` test helpers).
- [x] 2.2 Add `AiSettings.project_root: Option<PathBuf>` (default `None`) alongside it; verify the same default/sanitize behavior. 3 tests (default-none, round-trip, wrong-JSON-type-falls-back-to-none); every `AiSettings` struct-literal site updated again alongside `task_router_mode`'s own.
- [x] 2.3 Add `personas/task-router-rules.md` (initial content per `docs/fleet-snowfluff-feature-planning.md` §4.13's format example, in English per later direction) and a loader mirroring `persona_store::load()`; verify with a test that a missing or malformed rules file degrades gracefully (mix mode still runs, just without routing guidance) rather than failing the whole app. Landed as `crates/fleet-snowfluff/src/task_router_rules_store.rs` (file I/O, mirrors `persona_store.rs` exactly) + `BUNDLED_DEFAULT_TASK_ROUTER_RULES`/`with_task_router_rules` in `fleet-snowfluff-ai`'s `task_router.rs` (the pure logic — no parse step exists for plain text, so there's no genuine "malformed" case, only "missing/unreadable," which falls back to the bundled constant). Seeded at startup in `lib.rs` alongside `persona_store::seed_if_missing`. 4 file-I/O tests + 3 pure-logic tests, all passing.
- [x] 2.4 Implement escalation-marker detection as a pure function comparing an accumulating buffer against `<<ESCALATE>>` character-by-character (mismatch at any position = conclusively simple; full match = escalate; stream ends mid-prefix = simple); verify with unit tests covering: immediate mismatch, full match, and a reply shorter than the marker that never diverges (must resolve to simple, not an unresolved state). `detect_escalation`/`EscalationDecision` in `task_router.rs`, implemented via `str::starts_with` (already short-circuits at the first mismatching byte, no hand-rolled loop needed) with leading-whitespace trimming so a model that prepends a stray space isn't misread as conclusively simple. 6 tests, including a full match with leading whitespace and a genuine strict-prefix-then-stream-ends case.
- [x] 2.5 Restructure `run_generation` to act on 1.4's routed `ExecutionRoute`: when it points at the local Ollama profile, buffer that attempt's stream through the detector from 2.4 before forwarding anything to the UI; on conclusive-simple, flush the buffered prefix and continue forwarding the rest of the stream exactly as today; on escalate, discard everything buffered (never shown, never logged) and re-dispatch the same message directly to `default_profile` with the plain persona-only message list — **without calling `TaskRouter::route()` again** (see design.md's Decisions on why this is an execution-layer redirect, not a second routing decision). `single` mode's call path must be provably untouched. Verify with a test exercising both branches against a mock provider, and confirm the existing single-mode tests still pass unmodified. Landed as three functions in `chat_commands.rs`: `run_generation` (unchanged entry point for `single`/mix-direct-to-default/the fallback retry, now delegating its tail to...), `stream_to_completion` (the shared "read remaining chunks, forward, log, `Done`" tail, extracted so both a normal attempt and a mix-local attempt that resolved `Simple` can share it), and `run_generation_mix_local` (new — the buffer-then-decide loop). `send_chat_message` picks between `run_generation`/`run_generation_mix_local` by comparing `route.profile_key` against the Ollama/Local key, rather than threading the `ExecutionRoute` struct itself deeper into the call chain — its useful information (which profile) is already fully resolved into concrete values by that point. **No dedicated test for this**, consistent with this file's existing precedent (`subscription-first-chat` tasks 4.4/4.6): `chat_commands.rs` has no `#[cfg(test)]` module anywhere, since its functions take `AppHandle`/`Channel<ChatEvent>`/Tauri `State`, impractical to construct outside a running app. What _is_ verified: `cargo build --workspace` clean, `cargo test --workspace` 202 passed (up from 186)/0 failed/4 ignored confirming zero regressions to every existing (unrelated) test, `cargo clippy -p fleet-snowfluff --all-targets` produces no new warnings on this file, and the two pieces of real logic this depends on (`detect_escalation`, `with_task_router_rules`) are fully unit-tested at the `fleet-snowfluff-ai` layer (2.3/2.4 above). A live manual click-through of `mix` mode in `just dev` is a follow-up once Group 8's settings UI exists to actually turn it on — there's no way to set `task_router_mode` to `Mix` yet without hand-editing `ai-config.json`.
- [x] 2.6 Route infra-level local-profile failure (not enabled, `RuntimeUnavailable`, mid-stream error) through the same direct re-dispatch-to-`default_profile` path as an escalate (again, no second `TaskRouter::route()` call); verify with a test that a failing/unavailable local profile in `mix` mode still produces a normal reply via `default_profile`, with no failure shown to the user. "Not enabled" is actually handled one layer up, for free: `DefaultTaskRouter::route()` itself resolves straight to `default_profile` when `local_profile` is `None`, so `run_generation_mix_local` is simply never invoked in that case. `RuntimeUnavailable`/mid-stream error are the two cases `run_generation_mix_local` itself handles (its initial `chat()` call failing, and a stream error mid-loop), both routing to `run_generation_fallback`. Same "no dedicated test" note as 2.5 applies.
- [x] 2.7 Ensure only the winning attempt (never a discarded local attempt) is appended to the chat log; verify with a test that a discarded local attempt leaves no trace in `chat_log_store`. Satisfied by construction: `chat_log_store::append_entry` for the assistant reply is only ever called from `stream_to_completion`'s tail and from the "stream ended while still deciding" branch inside `run_generation_mix_local` (both only reachable _after_ a `Simple` determination) — the `Escalate` and infra-failure branches `return` through `run_generation_fallback` without ever calling `append_entry` for the discarded attempt. Same "no dedicated test" note as 2.5/2.6 — this is a structural guarantee (no code path exists that could log a discarded attempt), not something exercised by a test against a real `chat_log_store` file.
- [x] 2.8 Reconcile with the user-authored `personas/task-router-rules.md`: `BUNDLED_DEFAULT_TASK_ROUTER_RULES` now loads via `include_str!` from the real file (matching `persona::BUNDLED_DEFAULT_PERSONA_YAML`'s pattern) instead of a hand-typed literal. The file's own reply-format contract was rewritten to match `ESCALATE_MARKER`'s existing mechanism exactly (which was left unchanged, `<<ESCALATE>>`) rather than adapting the code to the file's initial draft wording: a `LOCAL` decision means just answer the user's message normally, in persona — not the bare word `LOCAL` — and an `ESCALATE` decision means the entire reply is exactly `<<ESCALATE>>`. No code-side preamble was added; the file is the single source of truth for both the classification criteria and the reply-format contract. Added a manual, `#[ignore]`d live test (`task_router_rules_live_classification_round_trip`) exercising one `LOCAL`-case and one `ESCALATE`-case prompt against a real local Ollama model, for hand-verifying edits to the rules file.

## 3. `ToolCallingProvider` trait and Ollama implementation

- [x] 3.1 Add `ToolCallingProvider: AiProvider` (`async fn chat_with_tools(&self, messages: Vec<Message>, tools: Vec<ToolDefinition>) -> Result<ToolCallStream, ProviderError>`) and `ToolCallStream`'s item enum (`TextDelta(String)` / `ToolCall{id, name, arguments}`) to `fleet-snowfluff-ai`; verify the trait is object-safe with a trivial test implementation, mirroring `AiProvider`'s own object-safety test. Landed as a new `tool_provider.rs` module (`ToolDefinition { name, description, parameters: Value }`, `ToolCallStreamItem`, `ToolCallStream`, `ToolCallingProvider`), exported from `lib.rs` alongside the rest. `TrivialToolProvider` test double proves `Box<dyn ToolCallingProvider>` is usable behind a trait object, mirroring `provider.rs`'s own `TrivialProvider`.
- [x] 3.2 Implement `ToolCallingProvider` for `Ollama`: a new request-building function (do not reuse `build_chat_body`'s signature as-is, since it needs a `tools` field added) that keeps `"think": false` — required, not just consistent with plain chat, because [ollama/ollama#10976](https://github.com/ollama/ollama/issues/10976) documents `think: true` combined with tool definitions producing empty output entirely for Qwen3 models — and a new response-parsing function distinct from `parse_ndjson_line` (which only deserializes `message.content`/`done` and would silently drop a `tool_calls` field). Verify by parsing literal fixture lines built from Ollama's own documented tool-calling response shape, and with a test asserting the request body sent has `"think": false` even when tools are present. Landed as `build_chat_with_tools_body`/`parse_ndjson_tool_line` in `ollama.rs` (the latter returns `Vec<ToolCallStreamItem>` rather than `Option<T>`, since a single NDJSON line can carry a text delta and one or more tool calls at once; Ollama's own tool-call objects have no `id` field, synthesized from array position instead). Streaming glue needed a new `http_stream::stream_lines_multi` generic function (the existing `stream_lines` is hardcoded to `StreamChunk`/`Option<T>`) — added alongside, not by rewriting, `stream_lines` itself, since three other providers already depend on it working exactly as before. 8 new tests (2 request-body, 6 line-parsing) plus 1 fixture built from Ollama's own documented tool-calling response shape (no `id` field on the tool call).
- [x] 3.3 Add the `RoutedExecution` enum (`PlainChat(Box<dyn AiProvider>)` / `ToolCapable(Box<dyn ToolCallingProvider>)`) and wire `build_provider()`'s existing match arms to return it, returning `ToolCapable` only for `(Ollama, Local)`; verify with a test asserting every other `(provider, auth_method)` combination returns `PlainChat`. Landed as a new `route_provider()` function in `ai_commands.rs` holding the (moved) match arms and returning `RoutedExecution`; `build_provider()` itself stays a thin `route_provider(...).into_ai_provider()` wrapper so its 8 existing plain-chat call sites (none of which need tool-calling yet — the Agent Loop that would doesn't exist until Group 4) need no changes. `into_ai_provider()` relies on `dyn` trait upcasting (stable since Rust 1.86, confirmed against this project's `rustc 1.97.1`) to coerce `Box<dyn ToolCallingProvider>` into `Box<dyn AiProvider>`. 2 new tests: every non-`(Ollama, Local)` combination asserted `PlainChat`, `(Ollama, Local)` asserted `ToolCapable`, plus `into_ai_provider` exercised for both variants.

## 4. Agent loop, `Tool` trait, and Tool Registry

- [x] 4.1 Add the `Tool` trait, `ToolContext { project_root, conversation_id }`, `ToolResult { content, is_error }`, and `PermissionTier` (`Auto`/`Confirm`/`Deny`) to the app crate; verify with a trivial test tool implementation exercising `required_permission`/`allows_session_remember`/`execute`. Landed as `crates/fleet-snowfluff/src/agent_tool.rs` (lives in the app crate, not `fleet-snowfluff-ai`, because `ToolContext` carries `ConversationId`); added a `ToolError(String)` type for `execute`'s error case (not specified in design.md's sketch but required by its own `Result<ToolResult, ToolError>` signature). Needed adding `async-trait` as a direct dependency of the app crate (previously only pulled in transitively via `fleet-snowfluff-ai`). 4 tests (`EchoTool`/`MinimalTool` doubles) covering the permission/remember/execute contract and the `allows_session_remember` default.
- [x] 4.2 Add the `AgentRuntime` trait and its Fleet(Aemeath) implementation: schema-validate a requested tool call, resolve its `required_permission`, execute if permitted, push the result back as a tool-result message, and loop until a final answer or a fixed max-iteration limit; verify with a test using mock tools that (a) a multi-tool-call turn resolves correctly, (b) iteration stops at the limit without hanging. Landed as `crates/fleet-snowfluff/src/agent_runtime.rs` (`AemeathAgentRuntime`, `max_iterations: 8` default, `ToolRegistry`, `PendingToolCall`, `PermissionDecider` trait so Group 6's real popup-backed decider is swappable for a trivial always-approve/always-deny one in tests). Required extending `fleet-snowfluff-ai::Message`/`Role` (a real gap found during implementation: the existing type was plain-text-only, with no way to represent an assistant's tool-call turn or a tool's result turn at all) — added `Role::Tool` and `Message.tool_calls: Vec<ToolCallRecord>` (`#[serde(default, skip_serializing_if = "Vec::is_empty")]`, so every existing plain message's wire format is byte-identical to before); `Anthropic::build_chat_body`'s exhaustive `Role` match gained an `unreachable!()` arm for `Role::Tool`, safe because `Role::Tool` only ever appears inside this loop's own `messages` vector and is never persisted to a conversation's session log (a real constraint documented on `Role::Tool` itself, load-bearing for later groups that wire the Agent Loop into `chat_commands.rs`). "Schema validation" resolved as a structural check (tool exists in the registry, arguments is a JSON object) rather than full JSON-Schema draft validation against each tool's `parameters` — no new dependency, and a malformed/unknown call resolves identically to a denied one: a tool-result message telling the model it didn't run. 5 tests via a `ScriptedProvider`/`CountingTool` test-double pair: multi-tool-call-turn resolution, iteration-limit termination (wrapped in a `tokio::time::timeout` as an extra "must not hang" guarantee, not just a structural one), a `Deny`-tier call never executing, a `Confirm`-tier call correctly deferring to the decider (both approve and deny paths), and an unknown tool call being reported without crashing.
- [x] 4.3 Add a basic `ContextManager` (`build_context`/`record_execution` only, per design.md — no compaction/memory-retrieval/sub-agent projection) that assembles the message list for a turn from conversation history and records the execution's outcome; verify with a test that it produces the same bounded-window behavior already guaranteed by the existing `ai-provider` "Bounded conversation context" requirement. Landed as `crates/fleet-snowfluff/src/context_manager.rs` (`AemeathContextManager`) — a thin wrapper: `build_context` is `chat_log_store::read_session` + `log::entries_to_context` + `prompt::assemble_messages`, exactly `send_chat_message`'s own existing assembly chain; `record_execution` is `chat_log_store::append_entry` with an `Assistant` `LogEntry`. Takes `session_path: &Path` rather than `AppHandle`/`ConversationId`, keeping it as Tauri-free/portable as `Tool`/`AiProvider`. 3 tests: output matches calling `assemble_messages` directly for a short history, the same for a history longer than `CONTEXT_WINDOW_TURNS` (proving the wrapper doesn't bypass capping), and `record_execution` appends a correctly-shaped entry.

## 5. Native tools

- [x] 5.1 Implement `web_search`: try a public SearXNG instance, fall back to the DuckDuckGo Instant Answer API on failure, return an honest `is_error: false` "no results" `ToolResult` if both fail; verify with tests covering the SearXNG-success, SearXNG-fails-DuckDuckGo-succeeds, and both-fail paths (mocked HTTP, no real network calls in the test suite). Landed as `native_tools/web_search.rs`: a `SearchTransport` trait abstracts the HTTP layer (mirrors `agent_runtime.rs`'s `ScriptedProvider` pattern) so the try-SearXNG-then-DuckDuckGo-then-honest-empty orchestration is tested against a `ScriptedTransport` double, no real network calls anywhere in the suite. Query strings are built via `reqwest::Url::query_pairs_mut()` rather than `RequestBuilder::query`, since that method needs reqwest's optional `query` feature (not enabled workspace-wide) and every other provider in this codebase already hand-builds its URLs. `Auto` permission tier (read-only, no side effects, same risk tier as the Claude Code/Codex `WebSearch` reasoning in Group 7). 9 tests: SearXNG-success, SearXNG-fails-then-DuckDuckGo-succeeds, SearXNG-empty-then-DuckDuckGo, both-fail, both-empty, unparseable-body-yields-no-results (not an error), plus the two parsers and the missing-argument case.
- [x] 5.2 Implement `read_file`/`list_directory`: canonicalize both `project_root` and the requested path, real containment check (not string-prefix) guarding against `..` traversal and symlink escapes; `required_permission` returns `Auto` inside `project_root`, `Confirm` otherwise (including when `project_root` is unset); verify with tests covering an in-root path, an out-of-root path, a `..`-traversal attempt, and a symlink pointing outside the root. Landed as `native_tools/file_tools.rs` (`ReadFileTool`/`ListDirectoryTool`): a relative path argument resolves against `project_root`, an absolute one is used as-is (the model may legitimately ask for a path outside the project directory, which is exactly what `Confirm` exists for); containment is `Path::canonicalize()` on both sides + `starts_with`, which resolves `..` components and symlinks alike, not a string-prefix check. Malformed/missing `path` arguments resolve to `Confirm`, never `Auto`. Symlink-escape test gated `#[cfg(unix)]` (matches `secrets_store.rs`'s existing platform-gating precedent) since Windows symlink creation needs elevated privileges. 9 tests total across both tools.
- [x] 5.3 Implement `run_command`: `sh -c`/`cmd /C` per OS, `stdin(Stdio::null())`, cwd hard-pinned to `project_root` (rejected the same way as an unconfigured root — no popup escalation to a different directory), `tokio::time::timeout` at 30s killing the child on expiry, stdout/stderr captured separately plus exit code, combined output capped at ~20KB with a truncation marker, `.kill_on_drop(true)` on the spawned `Child`, `allows_session_remember() == false` always; verify with tests for normal completion, timeout (a deliberately slow command), and output truncation. Landed as `native_tools/run_command.rs`; `required_permission` is `Deny` outright when `project_root` is unset (matching "no popup escalation" — there's no meaningful confirmation to offer) and `Confirm` for every command when a root is configured (shell execution is uniformly high-risk, unlike `read_file`'s in-root-is-`Auto` nuance). The 30s timeout is a configurable field (not a hardcoded constant) specifically so the timeout test can use a 200ms deadline against a real slow-spawned process instead of waiting 30 real seconds. `allows_session_remember` left at the trait's `false` default rather than overridden, satisfying the requirement without a redundant impl. 8 tests, including one real (fast) process spawn for normal completion and one for the timeout/kill path.
- [x] 5.4 Implement `get_system_context`: add the `sysinfo` dependency, return CPU/memory/uptime + current date/time (`chrono`) + `std::env::consts::OS`; verify with a test asserting the result contains all expected fields and that it does not attempt to read active-window title, idle time, or clipboard content. Landed as `native_tools/system_context.rs`; added `sysinfo = "0.39.6"` to the workspace (resolved via `cargo add --dry-run` rather than guessed, since its API has changed across major versions). `Auto` permission tier (read-only, no arguments). 4 tests, including one asserting the output text never mentions "window"/"idle"/"clipboard" — a real scope-leak regression, not a cosmetic one, given §9's fuller Context Awareness (Stage 6) does read those signals.
- [x] 5.5 Relocate `agent_tool.rs`, `agent_runtime.rs`, `context_manager.rs`, and `native_tools/` from the app crate into `fleet-snowfluff-ai`, alongside `ToolCallingProvider` (Group 3), per the user's explicit direction that the whole Agent Core belongs together in one crate. Every native tool's own implementation (subprocess spawning for `run_command`, an HTTP client for `web_search`, `std::fs` for `read_file`/`list_directory`) fits this crate's existing "things the Agent/AI layer does to execute a request" boundary at least as well as `ClaudeCodeCli`/`Codex`'s own subprocess spawning already does — this is not a new category of dependency for this crate, just more of the same kind it already had. Two dependency obstacles resolved: (1) `ToolContext` needs `ConversationId`, previously app-crate-only (`session_domain.rs`) — moved to a new `conversation.rs` module here, with `session_domain.rs` reduced to a `pub use fleet_snowfluff_ai::ConversationId;` re-export so every existing app-crate call site is unaffected; `ExecutionId`/`ExternalSessionRef` stay in the app crate (tied to `ChatRuntimeState`, a Tauri-runtime concept this crate has no reason to know about). (2) `ContextManager` needs to read/write a conversation's persisted session history, which needs `AppHandle`-resolved paths (`chat_log_store.rs`'s `chat_logs_dir`) — squarely app-crate territory; resolved via a new `SessionLog` trait (`read`/`append`) as the seam, implemented by a new `ChatLogStore` adapter in the app crate's `chat_log_store.rs` delegating to its own `read_session`/`append_entry`, so this crate never depends on the app crate. `AemeathContextManager` now takes `Box<dyn SessionLog>` (same shape as `WebSearchTool`'s `Box<dyn SearchTransport>`); its tests use an in-memory `SessionLog` instead of real files, since real file I/O is what the app crate's own `chat_log_store` tests already cover. `sysinfo`/`chrono`/`reqwest` moved (or were already present) in `fleet-snowfluff-ai`'s `Cargo.toml`; `sysinfo`/`reqwest` removed from the app crate's, no longer used there. `cargo test --workspace`: 186 passed in `fleet-snowfluff-ai` (up from ~142 before the move)/26 in the app crate, 0 failed, 5 ignored — confirming zero regressions.

## 6. Permission enforcement and tool-confirmation UI

- [x] 6.1 **(Gap found during review, not previously tracked)** Wire `AemeathAgentRuntime`/`ToolRegistry`/`ContextManager` into `chat_commands.rs`'s actual dispatch — today none of Groups 4/5's code is reachable from a real chat message: `send_chat_message` only ever calls `ai_commands::build_provider` (the `RoutedExecution`-discarding wrapper), never `route_provider` directly, so a `ToolCapable` (Ollama + `Local`) route never gets its tools. Needs: (a) use `route_provider` in place of `build_provider` where the route is Ollama/Local, and branch to a new `run_generation_with_tools`-style path when it resolves `ToolCapable`, keeping the existing `run_generation`/`run_generation_mix_local` paths untouched for `PlainChat`; (b) construct a `ToolRegistry` from the 4 native tools, with `ToolContext.project_root` from `AiSettings.project_root` and `ToolContext.conversation_id` from the already-resolved `ConversationId`; (c) decide and resolve the streaming mismatch this surfaces — `AgentRuntime::run` currently returns `Result<String, AgentError>` only once the whole tool-calling loop finishes, with no incremental `ToolCallStreamItem::TextDelta` forwarded to the caller mid-loop, unlike every existing path's real-time `ChatEvent::Chunk` streaming; either extend `AgentRuntime::run`'s signature to accept a sink/callback for streaming the final iteration's text deltas as they arrive, or explicitly accept buffered-until-done delivery for `mix`/tool-calling turns as a known, documented UX difference for this change — do not silently pick one without recording the decision in design.md; (d) decide whether to route persisted history through `AemeathContextManager::new(Box::new(chat_log_store::ChatLogStore))` (already built, unused) or keep `send_chat_message`'s existing inline `chat_log_store::read_session` + `log::entries_to_context` + `prompt::assemble_messages` sequence for now, deferring the swap to whenever `ContextManager` gets a second real consumer (design.md's own "no capabilities/RoutingReason fields yet, nothing to audit with only two destinations" reasoning applies equally here — don't introduce the indirection until something needs it). Verify with a live manual check in `just dev`: a `mix`-mode message routed to Ollama, with a tool-triggering prompt, actually executes a native tool and the reply reflects the tool's result.
  - **(a)** Landed as `route_provider` (now `pub(crate)` in `ai_commands.rs`, was private) plus a new `run_generation_routed` in `chat_commands.rs` that matches on `RoutedExecution` and dispatches to the existing `run_generation` (`PlainChat`) or a new `run_generation_with_tools` (`ToolCapable`); called from both places a message's *final* destination profile is decided (`send_chat_message`'s direct-to-`default_profile` branch, and `run_generation_fallback`). `run_generation_mix_local`'s own local-classification attempt is untouched and deliberately excluded — see its own doc comment for why (a separate, text-only escalation mechanism that doesn't yet interact with tool calling).
  - **A second, real bug found while wiring this in, fixed in the same pass**: `routed_to_local` compared only `route.profile_key == local_profile_key`, ignoring `mode` entirely. Since `single` mode always resolves to `default_profile` (`DefaultTaskRouter::route`), a user who sets *Ollama* as their `default_profile` (nothing prevents this) would have had every message incorrectly run through the mix-mode local-classification dance (`task-router-rules.md` injected, `<<ESCALATE>>` detection) even in `single` mode. Fixed by gating on `settings_snapshot.task_router_mode == TaskRouterMode::Mix` as well.
  - **(b)** Landed as-described: `ToolContext { project_root, conversation_id }` and a `ToolRegistry` built from all 4 native tools' `Default`/unit-struct constructors, both inline in `run_generation_with_tools`.
  - **(c)** Resolved by extending `AgentRuntime::run`'s signature with `on_text_delta: &mut (dyn for<'a> FnMut(&'a str) + Send)`, invoked for every `TextDelta` on *every* iteration (not just the final one) — a model that narrates before calling a tool still streams that narration live, exactly as if the tool call never happened; only tool-call JSON and tool execution stay invisible. `run_generation_with_tools`'s callback forwards straight to `channel.send(ChatEvent::Chunk{..})`/`partial_text`, matching every other path's existing streaming shape exactly. Required an explicit HRTB (`for<'a> FnMut(&'a str)`) rather than the natural elided form, to work around an `async_trait` macro-expansion lifetime quirk (confirmed via compiler error, not guessed) — the elided form bound the closure's `&str` parameter to the same fixed lifetime as the trait method's other reference parameters instead of a fresh per-call one. All 5 existing `agent_runtime.rs` tests updated to pass a no-op sink; added a new test (`text_deltas_stream_live_from_every_iteration_not_just_the_final_one`) proving text from a pre-tool-call iteration and the final iteration both reach the callback.
  - **(d)** Resolved as documented: kept the existing inline `chat_log_store::read_session`/`entries_to_context`/`assemble_messages` sequence; `ContextManager`/`ChatLogStore` remain unused by any real call site, deferred until a second real consumer exists.
  - The `PermissionDecider` passed to `AgentRuntime::run` here is a new `DenyAllConfirm` (denies every `Confirm`-tier call outright) — a deliberate, explicitly-documented fail-closed placeholder until tasks 6.2-6.4 land the real popup-backed decider, not a design decision. `Auto`-tier tools (`web_search`, `get_system_context`, in-`project_root` `read_file`/`list_directory`) work end to end today; anything needing confirmation (`run_command`, an out-of-root path) reports back to the model as "not permitted to run" until then.
  - `cargo test --workspace`: 187 passed in `fleet-snowfluff-ai` (up from 186)/26 in the app crate, 0 failed, 5 ignored. `cargo fmt --check`/`clippy --all-targets` clean. **The "live manual check in `just dev`" verification itself is not yet done** — recorded here as still outstanding rather than claimed; there is no UI control to set `task_router_mode: Mix` or `project_root` yet (that's Group 8), so exercising this end to end today needs hand-editing `ai-config.json`.
- [x] 6.2 Wire the agent loop (6.1) to batch every `Confirm`-tier tool call from one model turn and open a new `"tool-confirmation"` Tauri window (same `WebviewWindowBuilder`/`index.html`-reuse pattern as `status_bubble.rs`, `focused(true)`); verify by exercising the window-open path in a way that doesn't require a real display (unit-test the batching/collection logic separately from the actual window creation, matching how `status_bubble.rs` itself is tested if at all, or documenting why it's exercised manually per this project's existing untested-Tauri-glue precedent). Landed as a new `tool_confirmation.rs` (app crate): `PopupPermissionDecider` implements `PermissionDecider`, receiving the *whole* batch of `Confirm`-tier calls from one `AgentRuntime::run` iteration in a single `decide()` call (batching is already free here -- the loop only calls `decide()` once per iteration with everything pending, never per-call) and opening one popup for all of them together. Window creation/`WebviewWindowBuilder` itself is untested Tauri glue, matching `status_bubble.rs`'s own precedent; the batching/remember-partitioning logic (`partition_remembered`) is a separate, pure-ish function tested directly against a real `ChatRuntimeState` value, no window involved.
- [x] 6.3 Implement the `oneshot::Sender`/`Receiver` resolution: a Tauri command the confirmation window calls with the batch of accept/deny decisions, and the agent loop blocking on the paired receiver; closing the window without responding resolves as deny for every pending item in that batch; verify with a test simulating both a normal response and a dropped sender. Landed as `ToolConfirmationState` (holds at most one in-flight `PendingBatch{items, responder}` -- `ai-chat`'s "Single in-flight generation" already guarantees only one confirmation round is ever active) plus two commands: `get_pending_tool_confirmations` (frontend reads what to show) and `resolve_tool_confirmations` (frontend submits decisions, resolving the oneshot). Closing the window without responding is handled by a `WindowEvent::Destroyed` handler that drops `state.pending` (and so the `Sender`) if it's still `Some` -- `confirm_via_popup`'s `rx.await.unwrap_or_default()` then yields an empty response list, approving nothing. 2 tests exercising the oneshot channel directly (normal send, dropped sender) without any Tauri window.
- [x] 6.4 Add the session-scoped remember allowlist to `ChatRuntimeState`, keyed by `(ConversationId, ToolName)` for path-less tools and `(ConversationId, ToolName, path)` for `read_file`/`list_directory`; consult it before opening the confirmation window (a hit means `Auto`, skip the popup entirely); clear it in `new_chat_session` on the same lifecycle as `cli_sessions`; verify with a test that approving one path doesn't grant a different out-of-root path, and that the allowlist is empty after `new_chat_session`. Landed as a new `RememberKey` enum (`Tool(String)` / `ToolPath(String, String)`) in `chat_commands.rs`, derived generically from whether a call's own arguments contain a `"path"` field (`RememberKey::for_call`) rather than a hardcoded tool-name list, so any future path-scoped tool gets the same protection for free. `ChatRuntimeState` gained `remembered_tools: Mutex<HashSet<(ConversationId, RememberKey)>>` plus `is_tool_remembered`/`remember_tool` methods, cleared in `new_chat_session` alongside `cli_sessions`. `partition_remembered` (in `tool_confirmation.rs`) consults it before ever building a popup item, and only a `response.approved && response.remember` choice writes a new entry back. 2 dedicated tests (remembered call skips the popup entirely; a different out-of-root path for the same tool still needs a real decision).
- [x] 6.5 Set `allows_session_remember() == false` for `run_command` and any write-capable tool, `true` for the read-only tools; verify with a test asserting the confirmation UI never offers the remember option for `run_command`. The `Tool` implementations themselves were already correct (Group 5); what this task actually adds is the UI-facing check that the confirmation popup *respects* that value -- `build_item`'s `allows_remember` field is populated directly from `tool.allows_session_remember()`, verified with a test constructing a `run_command`-shaped pending item and asserting `allows_remember == false`.
- [x] 6.6 Confirm (via a live manual check, `just dev`) that the chat window and status bubble require no code changes to keep showing "processing"/`...` while a generation is blocked on the confirmation window — this should already work via `ChatRuntimeState.pending` per design.md; record the result in this task rather than adding new state if it's confirmed unnecessary. **Not manually click-tested in this environment** (no display available) -- recorded here as reasoned-through rather than observed: `run_generation_with_tools` (which calls `PopupPermissionDecider::decide`, which can block on `confirm_via_popup`'s `rx.await` for an arbitrarily long time) runs entirely inside the same `tokio::spawn`ed task whose `JoinHandle` is stored in `ChatRuntimeState.pending` for the whole duration, exactly like every other generation path; `clear_pending` is only ever called once that task reaches its own end (`Ok`/`Err` branch after `runtime.run(...)` returns). `get_chat_state`'s `is_pending` and the bubble's `...` glyph read exactly that same field, so no new state was needed, matching design.md's prediction -- but this is inference from reading the code paths, not an observed manual test, and should be spot-checked in `just dev` before this change is archived.

## 7. Claude Code / Codex native-tool by-case allow-list

- [ ] 7.1 Replace `claude_code_cli.rs`'s blanket `DISALLOWED_TOOLS` constant with a per-profile setting built into `--allowedTools`/`--disallowedTools` at spawn time; verify with a test asserting the constructed argument list matches the expected allow/deny split for a given setting.
- [ ] 7.2 Add the default split (`Read`/`Glob`/`Grep`/`WebSearch`/`WebFetch` = auto, `Write`/`Edit`/`Bash`/`NotebookEdit`/`Task`/`SlashCommand` = deny) to `AiSettings` (or a per-profile sub-setting), user-adjustable; verify defaults with a test.
- [ ] 7.3 Leave Codex's tool-access control at its existing coarse `--sandbox` level (no attempt at per-tool granularity — unverified/experimental per design.md's Risks); document this gap explicitly rather than silently matching Claude Code's UI.

## 8. Settings UI

- [ ] 8.1 Add the `task_router_mode` control to the AI tab (single/mix), reflecting current state on open and applying immediately; verify with `tsc --noEmit` and a manual check that toggling it persists across a settings-window reopen.
- [ ] 8.2 Add the `project_root` folder-picker control (native dialog) to the AI tab, showing "not configured" plainly when unset; verify with a manual check that picking a folder persists and round-trips on reopen.
- [ ] 8.3 Add the native-tool allow-list controls for Claude Code/Codex profiles (per 7.2's defaults, user-adjustable); verify with `tsc --noEmit` and `npm run build`.
- [ ] 8.4 Add any new locale keys for the above to all 5 locale files identically; verify `i18n::tests::every_locale_parses_and_shares_the_same_key_set` still passes.

## 9. Verification

- [ ] 9.1 Run `cargo test --workspace` and confirm 0 failures, noting the new pass count.
- [ ] 9.2 Run `tsc --noEmit` and `npm run build` clean.
- [ ] 9.3 Manually verify in `just dev`: `single` mode behaves identically to today; `mix` mode routes a simple message to Ollama and a message that should escalate to `default_profile`; a `run_command`/`read_file` call outside `project_root` produces the confirmation popup; approving "remember this session" for a low-risk tool suppresses the popup on a repeat call; the chat window/bubble show "processing" throughout.
- [ ] 9.4 Run `openspec validate --changes agent-core-and-task-router --strict` and resolve any findings before archiving.
